# ops-erp rate-limit zones — must be loaded at the http{} context.
# Deploy to /etc/nginx/conf.d/ops-erp-http.conf
limit_req_zone $binary_remote_addr zone=ops_erp_api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=ops_erp_auth:10m rate=3r/s;
limit_req_status 429;
